<div class="title">{{ type }}</div>
<ul class="classes">
  {{#each units}}
  <li class="character {{ name }}">
    <div class="title">{{ name }}</div>
    <div class="portrait"></div>
  </li>
  {{/each}}
</ul>
